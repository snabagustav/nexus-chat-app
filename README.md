# Nexus Chat App

Chat, rooms, calls, screen sharing, admin panel, themes and games.

Railway variables:

- `JWT_SECRET`
- `NODE_ENV=production`

Optional:

- `ADMIN_PIN=5693`
- `ADMIN_EMAIL=gustavenglund69@gmail.com`
