# Nexus Chat

Self-hosted chat, calls, admin menu and games.

## Railway variables

Set these in Railway:

- `JWT_SECRET`: any random text
- `NODE_ENV`: `production`

Optional:

- `ADMIN_PIN`: default is `5693`
- `ADMIN_EMAIL`: default is `gustavenglund69@gmail.com`

## Admin

Log in, click the `A` button, enter PIN `5693`.
